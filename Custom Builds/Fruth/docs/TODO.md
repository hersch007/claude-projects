# TODO — Recommended Next Steps

Ordered roughly by priority. Nothing in this list touches pricing formulas — per `docs/decisions.md`, business-logic changes wait for explicit approval, and the validation in `FORMULA-VALIDATION.md` found none needed.

## Immediate / Low-Risk Fixes

- [x] **Test the Start Performance Platform integration on a real Start Performance instance.** ~~This entire feature is inert on the live Fruth Custom Packaging site...~~ **Confirmed fully working live** by the client on a real Start Performance instance (`sp-fruth`): "Fruth Quotes" and "Fruth Pricing Data" both show correctly under the Sales Core sidebar section, quote save/load works, pricing matches the Excel workbook once comparing the same film-type scenario (an earlier "doesn't match" report turned out to be comparing "PE/PP Clear" in Excel against "PE/PP Color (Tint)" in the app, not a calculation bug), and the embedded toolbar sits flush at the top of the page with no gap, verified on both desktop and narrow viewports. Took several rounds to get fully right (8.9.1: KPI row crashing to $0.00 when embedded; 8.9.2: toolbar/modal completely unstyled because its CSS is normally printed via `wp_head`, which Start Performance's shell never fires; 8.9.3-8.9.6: the sticky-toolbar top-offset math, including one reverted approach -- see `docs/CHANGELOG.md` for the full blow-by-blow). Closed.
- [x] **Investigate a rounding discrepancy between the app and the Excel workbook.** ~~Needs a specific example...~~ **Diagnosed and closed**: client supplied a concrete example (In-Line BSB, Price/Thousand $578.54 in the app vs. $149.61 in Excel for apparently matching inputs). Ran the exact inputs through the plugin's real pricing engine via `tests/extract-pricing-engine.js` -- confirmed the formula itself is correct (with Custom Setup Charge at $0 the engine produces $147.29-$149.61, matching Excel to within the known resin-cost difference). The $450 in the field turned out to be In-Line's built-in example-scenario default (`DATA.defaults.inline.customSetupCharge`), which 8.9.11's `freshForms()` never clears on a new quote -- not a rounding or formula bug. **Shipped in 8.9.13**: the results table visually flags Custom Setup Charge / Setup Charge rows (amber highlight + label) whenever that override is active. **Shipped in 8.9.14**: Custom Setup Charge and COGS Overhead % are now real editable inputs on the Tubing and In-Line forms (COGS Overhead % on Zipper too) -- previously the only way to change either was the admin-only Data Editor's global "Calculator Defaults," so a rep had no way to correct a bad per-quote value at all. See `docs/CHANGELOG.md` 8.9.13 and 8.9.14.
- [x] **Resin Cost/LBS silently stale when Formula is left at its pre-selected default.** Found manually testing the 8.9.15 pricing update: a Tubing quote using FCR-1000 showed the wrong total because Resin Cost/LBS never auto-filled -- the Formula dropdown pre-loads with a formula already selected on a new quote, so leaving it untouched fires no `change` event and `syncResinCostFromFormula()` never runs, leaving Resin Cost/LBS at its own separate (and in this case outdated) default. **Shipped in 8.9.16**: new/reset quotes now start with Formula unselected and Resin Cost/LBS blank, so picking any formula is always a real change event. See `docs/CHANGELOG.md` 8.9.16.
- [x] **The Kris pricing update (8.9.15) never actually reached `sp-fruth`, even on v8.9.16.** After the 8.9.16 fix above, FCR-1000 still auto-filled the old pre-Kris cost (1.039, not 1.1278). Root cause: pricing tables are database-backed once a site has been activated -- `SQS_DEFAULT_DATA_B64` only fills in rows the database doesn't already have, and never overwrites an existing one (see `docs/decisions.md`, "Important operational fact" section, for the full mechanism). Uploading a new zip was never going to apply a pricing-data change to an already-live site. **Resolved by hand**: the client updated the 11 changed Formula Costs values directly in the live "Fruth Pricing Data" Data Editor on `sp-fruth` and removed the two discontinued formula rows; confirmed live via T1 (FCR-1000 now correctly auto-fills 1.1278, CFB-1000 confirmed at 1.08). **Confirmed**: client ran all 10 scenarios in `docs/Manual-Test-Scenarios.xlsx` against the live `sp-fruth` app after the Data Editor fix -- all match. `sp-fruth` is fully closed out on this issue. **Standalone Fruth site (`startwebservicesbackup.com/fruth/`) intentionally excluded**: client decision -- that site is grandfathered on the original (pre-Kris) Formula Costs and will *not* get this Data Editor update. Not an oversight; do not apply the Kris values there unless the client explicitly asks otherwise.

- [ ] **Test the User Accounts + front-end User Management features (Core 8.7.0 → 8.8.0) on staging before relying on them in production.** Not yet verified end-to-end in a live WordPress/MySQL environment (no PHP interpreter or live DB available in this working environment — reviewed carefully by hand instead, including full diffs against both 8.6.10 and 8.7.0, and re-runs of the 81-assertion pricing regression suite, all clean). Before treating this as done: upload the zip, confirm the migration runs cleanly and creates two "(legacy)" rows in `wp_sqs_users`, confirm both existing shared logins still work unchanged, confirm the Customer Database module's session-based access still works without any change to that plugin, add one brand-new individual account through the wp-admin Login & Branding UI and confirm role-appropriate access, and deliberately test the "Reset Logins" recovery path once. **For 8.8.0 specifically**: give a test account `can_manage_users=1`, confirm it can log into the new front-end `[sqs_user_admin]` page (reached via the Portal's new "User Management" tile) and fully manage other accounts there with no WordPress login involved; confirm the two lockout guards independently prevent removing the last active admin-role account and, separately, the last active `can_manage_users` account; confirm logging into the Sales Portal in one browser tab and User Management in another (same browser session) doesn't cross-contaminate the Quote Builder's "Prepared By" auto-fill. This is the highest-risk change shipped this session (touches login/session code directly, and is the first time User Management AJAX is reachable by anonymous front-end visitors) — see `docs/CHANGELOG.md` 8.7.0 and 8.8.0 for the full change lists.
- [x] **Test the Customer Database module on staging.** ~~Needs confirming...~~ **Confirmed working live** (tested directly via browser automation against `https://startwebservicesbackup.com/fruth/`, plus client confirmation after uploading v1.3.2): Portal's 3rd tile, Customer Database CRUD page, City/State/ZIP/Assigned Rep fields save and display correctly, "Start Quote" button correctly pre-fills a new quote, the autofill fix (Prepared By/Internal Reference correctly blank on a fresh quote), Save Quote → customer gets linked → quote count updates → Delete becomes correctly blocked, Quote History modal shows the linked quote. One real bug found and fixed during this pass — see 1.3.2 in `docs/CHANGELOG.md` (the "+ Add New Customer" quick-add wasn't opening due to a JS event-bubbling timing bug; fixed with `e.stopPropagation()`). Closed.
- [x] **Decide what to do about the duplicate "Print Quote" button.** ~~Recommend removing Core's native `#printBtn`...~~ **Shipped in 8.6.9**: removed Core's native `#printBtn` (bare `window.print()` on the raw UI, which risked printing internal margin/cost data) and kept the Print Module's `#sqsPrintBtn` (the one that renders a proper formatted quote card first). Client decision: no duplicates. Regression suite re-run clean (81/81), diff confirmed scoped to exactly the removed button + handler. Closed.

- [x] **Fix the invisible "Add Row" / "Advanced View" buttons in the Data Editor.** ~~Root cause confirmed: `sales-quote-system.php`, the `.sqs-data-mini` CSS rule (inline `<style>` block, ~line 1018) is missing a `color` declaration~~ — **Shipped in 8.6.3**: added `color:#172033;` to `.sqs-data-mini`, matching `.sqs-data-app`'s base text color. See `plugins/core/sales-quote-system-v8.6.3-fruth.zip`. Regression suite re-run against this build (81/81 pass) confirms no pricing impact. **Confirmed fixed on staging** — buttons are readable now. Closed.
- [x] **Fix the Portal page's live URL.** ~~Code is correct (`sqs_pricing_calculator_portal_url()` dynamically finds whichever page has `[sqs_portal]`); the WordPress *page* is currently published at `/fruth/` instead of `/fruth/portal/`.~~ **Confirmed fixed** — Portal page is now published at `/fruth/portal/` in WP admin. Closed.
- [ ] **Get the `FIS-5100RD (Red PP)` missing cost from Fruth** — it's a gap in the source Excel `Resin Prices` sheet, not something the plugin can infer. **Still deferred**: the newer "Kris Hall" workbook (adopted in Core 8.9.15) has the same gap — `FIS-5100RD (Red PP)` is still unpriced there too, so the meeting with Fruth apparently didn't resolve this yet, or Kris's sheet hasn't caught up. Two other formulas from the original list (`FIS-3000BK (Black Cond)`, `FIS-1010 (Zipper)`) disappeared from Kris's workbook entirely with no replacement cost and were removed from the app in 8.9.15 per client direction — worth asking Fruth directly whether those are truly discontinued or just an omission, same as this one. No code action possible on any of the three until a real number (or explicit confirmation of removal) comes back.

## Consistency / Robustness (Low Risk, Recommend Before Next Feature Work)

- [x] **Unify the three navigation-URL functions on one resolution strategy.** ~~`sqs_pricing_calculator_portal_url()` dynamically resolves via shortcode lookup; the other two are hardcoded string literals.~~ **Shipped in 8.6.4**: added a shared helper, `sqs_pricing_calculator_shortcode_page_url( $shortcode_tag, $fallback_path )`, and all three URL functions now use it. Quote Builder and Editor URLs now self-heal if their pages move, falling back to the previous hardcoded path only if no matching page exists. No behavior change on the current live site (both pages already exist at the expected slugs). Regression suite re-run clean (81/81).
- [x] **Keep `SQS_VERSION` / `SQSP_VERSION` constants in lockstep with the plugin header version.** **Shipped in 8.6.4**: added `tests/check-plugin-versions.js`, which parses both plugins' header `Version:` line and internal constant and fails loudly on any mismatch — the exact bug that shipped in Core 8.6.1 and Print 1.14.1. Also added an inline comment at each `define()` site pointing at the check. Run alongside the pricing suite before any release.
- [x] ~~**Consider hardening the `wp_ajax_nopriv_*` quote endpoints** — dropping `nopriv` and relying on the session check alone would shrink the attack surface with no functional change for actual users.~~ **Corrected, not implemented.** This recommendation was wrong: this plugin's sales/admin login is a custom PHP `$_SESSION` flag only — `wp_set_current_user()` / `wp_signon()` are never called anywhere in the codebase, confirmed by grep. That means every real sales rep and admin using the tool is, from WordPress's perspective, a logged-out visitor, and `wp_ajax_nopriv_sqs_*` is what makes their AJAX calls reach the handler at all. Removing it would have broken quote save/load for everyone except WordPress `manage_options` users. The nonce + session-flag check already in place (`sqs_pricing_calculator_user_can_quote_ajax()` + `check_ajax_referer()`) is the real and necessary security boundary here — no change recommended.

## Regression Suite Expansion

A working suite exists now: `tests/pricing-regression.test.js` (dependency-free Node, extracts the real plugin JS via `tests/extract-pricing-engine.js` and checks it against Excel-derived values — see `tests/README.md`). It currently covers one representative scenario per calculator plus four edge cases (setup-fee floor, custom-charge override, a 10,000-unit run, and the margin-on-price identity). Recommended expansion, in order:

- [x] Add one fixture per **quantity-break tier** (2/4/8/24/48 production hours) for all three calculators — done in `tests/pricing-regression.test.js` (`runPriceBreakCase`). Tubing: all 5 tiers match Excel `F16:J20` exactly. In-Line BSB: all 5 tiers verified (values recalculated per the known `B15` staleness noted in `FORMULA-VALIDATION.md`, hand-confirmed for 2 tiers). Zipper: 4 of 5 tiers (4/8/24/48hr) match Excel `G16:J19` exactly; the 2-hour tier is excluded because Excel's own `F16` cell is a hardcoded number, not a formula — see `FORMULA-VALIDATION.md` for the full note. 81 assertions total, all passing.
- [ ] Add a **zero/blank-input** edge case per calculator (what happens at qty=0, gauge=0) to lock in current behavior before any refactor touches it.
- [ ] Add a case per **film type / resin type combination** at least once each, since `setupLbsTable` has `"NA"` cells for some (Nylon at wide buckets) that resolve to a special-cased 0 in the JS — worth a dedicated assertion rather than relying on the one scenario per calculator to happen to exercise it.
- [ ] Wire `tests/pricing-regression.test.js` into whatever CI/build step eventually gets set up (currently there is none — see below) so it runs on every change automatically rather than only when someone remembers to run it.
- [ ] As the Print Module gains real logic (currently zero functional differences across its three versions), add print-output assertions confirming totals shown in the printed quote match the Quote Builder's own totals — called out as a requirement in `docs/testing.md` ("Quote totals match Quote Builder").

## Documentation Debt

- [ ] `docs/architecture.md`, `docs/decisions.md`, `docs/Known-Issues.md`, `docs/Release.md`, `docs/requirements.md`, and `docs/testing.md` are currently terse outline-style notes (bullet fragments, not prose). They're accurate as far as they go but don't reflect the level of detail now captured in `PROJECT-SUMMARY.md` and `FORMULA-VALIDATION.md`. Consider merging/expanding rather than maintaining two tiers of documentation going forward.
- [ ] No `ARCHITECTURE.md` exists yet despite being named in `docs/requirements.md`'s list of files Claude should maintain — `docs/architecture.md` (lowercase, existing) currently serves that role as a bare outline. Recommend either renaming/expanding it or treating `PROJECT-SUMMARY.md` §2 as the architecture reference and retiring the stub.

## Longer-Term (Phase 2 per `docs/feature-roadmap.md`)

Not started, no code present for any of these — listed here only to confirm they're correctly absent, not overlooked:
- Quote numbering beyond the current simple sequential format (`SQS-YYYY-NNNNN`) — already implemented at a basic level (`sqs_pricing_calculator_generate_quote_number()`); revision tracking also already exists (`Rev A`, `Rev B`, ... via `sqs_pricing_calculator_revision_from_index()`). Full "saved quotes" browsing UI exists (`sqs_load_quotes`/`sqs_get_quote` AJAX + Load Quote button); a dedicated quote-history/search page does not.
- ~~Customer database~~ **Built and confirmed working live this session** (`quote-builder-customers` v1.3.2).
- ~~Individual user accounts and roles~~ **Built this session** (Core `sales-quote-system` v8.7.0) — replaces the two shared Sales/Editor logins with real per-person accounts (name, username, password, role, active/inactive), managed from the Login & Branding admin page. Migration from the old shared logins runs automatically; a break-glass "Reset Logins" recovery path was kept. Not yet verified on live staging — needs real end-to-end testing before production use, especially the automatic migration step (see `docs/CHANGELOG.md` 8.7.0 for full detail).
- **Follow-up noted, not built**: a real `created_by_user_id` audit column on `wp_sqs_quotes`, set server-side from the session at save time. Now that real per-person identity exists (as of 8.7.0), this would give a tamper-proof "who actually created this quote" record — today's new "Prepared By" auto-fill is a display convenience only (free text, editable, not a foreign key). Low risk, natural next step once the accounts feature itself is confirmed stable in production.
- PDF generation (Print Module currently renders an in-browser printable view only, no PDF export).
- Email quote delivery.
- Approvals workflow.
- Reporting/margin-analysis dashboard.
- CRM/ERP integration.

## Process Note

No build tooling, package manager, or test runner currently exists in this repo. `tests/pricing-regression.test.js` was deliberately written with zero npm dependencies so it works immediately, but if the project grows a real build step, consider adding a minimal `package.json` with a `test` script (`node tests/pricing-regression.test.js`) so `npm test` works as expected for future contributors.
