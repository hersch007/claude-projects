Do not redesign this application simply because there is a "better" way.

Do not replace working business logic.

Do not rewrite the architecture unless requested.

Prefer small, incremental improvements.

Keep commits focused and easy to test.

Working software takes precedence over elegant code.